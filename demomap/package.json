{
  "name": "demomap",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host localhost --port 5173",
    "dev:80": "vite --host localhost --port 80",
    "build": "vite build",
    "preview": "vite preview --host localhost --port 4173"
  },
  "dependencies": {
    "@giro3d/giro3d": "^2.0.0-dev",
    "ol": "^10.8.0",
    "proj4": "^2.19.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
